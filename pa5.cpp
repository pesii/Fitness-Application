#include "pa5.h"
#include "DietPlans.h"
#include "ExercisePlan.h"


/*
// input stream
ifstream & operator>> (ifstream &in, DietPlan &data) 
{
	int input = 0;
	input >> in;
	data.set_goal(input);
	
	string date_input;
	date_input >> in;
	data.set_date(date_input);

	string name_input;
	in >> name_input;
	data.set_name(name_input);
}
*/
